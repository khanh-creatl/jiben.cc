local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")
local LocalPlayer = Players.LocalPlayer
local Camera = game.Workspace.CurrentCamera

-- Biến trạng thái
local CamLockEnabled = false
local LockedPlayer = nil
local Gui = nil
local Ping = 150 -- Ping mặc định, sẽ cố lấy giá trị thực
local SmoothingFactor = 0.98 -- Gần tức thời, giữ chút mượt
local TargetPartName = "Head" -- Khóa vào đầu
local HeadOffset = Vector3.new(0, 0.2, 0) -- Offset nhỏ để nhắm chính xác vào đầu

-- Tạo GUI
local function CreateGUI()
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Parent = game.CoreGui
    ScreenGui.Name = "DNKCamLock"

    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(0, 150, 0, 100)
    Frame.Position = UDim2.new(0.5, -75, 0.5, -50)
    Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Frame.BackgroundTransparency = 0.5
    Frame.Parent = ScreenGui
    Frame.Active = true
    Frame.Draggable = true

    local Title = Instance.new("TextLabel")
    Title.Size = UDim2.new(1, 0, 0, 30)
    Title.Position = UDim2.new(0, 0, 0, 0)
    Title.BackgroundTransparency = 1
    Title.Text = "DNK CamLock"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextScaled = true
    Title.Font = Enum.Font.SourceSansBold
    Title.Parent = Frame

    local ToggleButton = Instance.new("TextButton")
    ToggleButton.Size = UDim2.new(0.8, 0, 0, 40)
    ToggleButton.Position = UDim2.new(0.1, 0, 0.5, 0)
    ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    ToggleButton.Text = "OFF"
    ToggleButton.TextColor3 = Color3.fromRGB(255, 0, 0)
    ToggleButton.TextScaled = true
    ToggleButton.Font = Enum.Font.SourceSans
    ToggleButton.Parent = Frame

    local function ChangeTitleColor()
        while true do
            local tweenInfo = TweenInfo.new(1, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
            local colors = {
                Color3.fromRGB(255, 0, 0),
                Color3.fromRGB(0, 255, 0),
                Color3.fromRGB(0, 0, 255),
                Color3.fromRGB(255, 255, 0)
            }
            for _, color in ipairs(colors) do
                TweenService:Create(Title, tweenInfo, {TextColor3 = color}):Play()
                wait(1)
            end
        end
    end
    spawn(ChangeTitleColor)

    ToggleButton.MouseButton1Click:Connect(function()
        CamLockEnabled = not CamLockEnabled
        ToggleButton.Text = CamLockEnabled and "ON" or "OFF"
        ToggleButton.TextColor3 = CamLockEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
        if not CamLockEnabled then
            LockedPlayer = nil
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Highlight") then
                    player.Character.Highlight:Destroy()
                end
            end
        end
    end)

    Gui = ScreenGui
end

-- Tạo Highlight
local function CreateHighlight(character)
    local highlight = Instance.new("Highlight")
    highlight.FillColor = Color3.fromRGB(0, 255, 0)
    highlight.OutlineColor = Color3.fromRGB(0, 255, 0)
    highlight.FillTransparency = 0.5
    highlight.OutlineTransparency = 0
    highlight.Adornee = character
    highlight.Parent = character
    highlight.Enabled = true
    return highlight
end

-- Tìm mục tiêu gần tâm màn hình
local function FindTarget()
    local closestPlayer = nil
    local closestScreenDistance = math.huge
    local maxDistance = 500 -- Giới hạn khoảng cách thực tế (studs)
    local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local targetPart = player.Character:FindFirstChild(TargetPartName)
            if targetPart then
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position + HeadOffset)
                local distance = (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) and 
                    (targetPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude or math.huge
                local screenDistance = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude

                if onScreen and distance < maxDistance and screenDistance < closestScreenDistance then
                    closestScreenDistance = screenDistance
                    closestPlayer = player
                end
            end
        end
    end
    return closestPlayer
end

-- Theo dõi hồi sinh
local function MonitorPlayerRespawn(player)
    if not player then return end
    player.CharacterAdded:Connect(function(newCharacter)
        if CamLockEnabled and player == LockedPlayer then
            local humanoid = newCharacter:WaitForChild("Humanoid", 5)
            local targetPart = newCharacter:WaitForChild(TargetPartName, 5)
            if humanoid and targetPart and humanoid.Health > 0 then
                if not newCharacter:FindFirstChild("Highlight") then
                    CreateHighlight(newCharacter)
                end
            end
        end
    end)
end

-- Dự đoán vị trí mục tiêu
local function PredictTargetPosition(targetPart)
    local targetPos = targetPart.Position + HeadOffset
    local velocity = targetPart.Velocity
    local predictionFactor = (Ping / 1000) * 1.2 -- Tăng hệ số để đoán trước xa hơn
    if velocity.Magnitude > 0 then
        -- Dự đoán dựa trên vận tốc và hướng
        targetPos = targetPos + velocity * predictionFactor
        -- Thêm offset dựa trên hướng di chuyển
        local direction = velocity.Unit
        targetPos = targetPos + direction * (predictionFactor * 0.5) -- Đẩy mục tiêu xa hơn theo hướng
    end
    return targetPos
end

-- Phím tắt điều chỉnh tốc độ
UserInputService.InputBegan:Connect(function(input, gameProcessedEvent)
    if not gameProcessedEvent then
        if input.KeyCode == Enum.KeyCode.Equals then -- Phím "+" tăng tốc
            SmoothingFactor = math.min(SmoothingFactor + 0.02, 1)
            print("SmoothingFactor:", SmoothingFactor)
        elseif input.KeyCode == Enum.KeyCode.Minus then -- Phím "-" giảm tốc
            SmoothingFactor = math.max(SmoothingFactor - 0.02, 0.1)
            print("SmoothingFactor:", SmoothingFactor)
        end
    end
end)

-- Xử lý CamLock
local lastCFrame = Camera.CFrame
RunService.RenderStepped:Connect(function(deltaTime)
    if CamLockEnabled and LocalPlayer.Character then
        -- Cố lấy ping thực tế
        if game:GetService("NetworkClient") then
            Ping = game:GetService("NetworkClient"):GetPing() or Ping
        end

        -- Tìm mục tiêu nếu không có hoặc không hợp lệ
        if not LockedPlayer or not LockedPlayer.Character or not LockedPlayer.Character:FindFirstChild("Humanoid") or LockedPlayer.Character.Humanoid.Health <= 0 then
            if LockedPlayer then
                MonitorPlayerRespawn(LockedPlayer)
            else
                LockedPlayer = FindTarget()
                if LockedPlayer then
                    MonitorPlayerRespawn(LockedPlayer)
                end
            end
        end

        -- Khóa camera vào mục tiêu
        if LockedPlayer and LockedPlayer.Character and LockedPlayer.Character:FindFirstChild(TargetPartName) and LockedPlayer.Character.Humanoid.Health > 0 then
            local targetPart = LockedPlayer.Character:FindFirstChild(TargetPartName)
            local targetPos = PredictTargetPosition(targetPart)
            local targetCFrame = CFrame.new(Camera.CFrame.Position, targetPos)
            lastCFrame = lastCFrame:Lerp(targetCFrame, SmoothingFactor)
            Camera.CFrame = lastCFrame

            -- Thêm highlight
            if not targetPart.Parent:FindFirstChild("Highlight") then
                CreateHighlight(targetPart.Parent)
            end
        else
            -- Xóa highlight cho các nhân vật khác
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LockedPlayer and player.Character and player.Character:FindFirstChild("Highlight") then
                    player.Character.Highlight:Destroy()
                end
            end
        end
    else
        -- Xóa highlight khi tắt CamLock
        for _, player in pairs(Players:GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("Highlight") then
                player.Character.Highlight:Destroy()
            end
        end
    end
end)

-- Khởi tạo GUI
CreateGUI()

-- Thông báo khởi động
StarterGui:SetCore("SendNotification", {
    Title = "DNK CamLock",
    Text = "CamLock Loaded! Aims at head, use +/- to adjust speed.",
    Duration = 5
})

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")
local Stats = game:GetService("Stats")
local LocalPlayer = Players.LocalPlayer
local Camera = game.Workspace.CurrentCamera

-- Biến trạng thái
local CamLockEnabled = false
local LockedPlayer = nil
local Gui = nil
local LastTargetPosition = nil
local PredictionFactor = 0.35 -- Dự đoán mạnh
local TargetPartName = "Head" -- Khóa vào đầu
local FOVRadius = 200 -- Phạm vi POV (dựa trên vùng khoanh vàng)
local StealthMode = false -- Chế độ POV tàng hình

-- Tạo GUI
local function CreateGUI()
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Parent = game.CoreGui
    ScreenGui.Name = "DNKCamLock"

    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(0, 150, 0, 150)
    Frame.Position = UDim2.new(0.5, -75, 0.5, -75)
    Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Frame.BackgroundTransparency = 0.5
    Frame.Parent = ScreenGui
    Frame.Active = true
    Frame.Draggable = true

    local Title = Instance.new("TextLabel")
    Title.Size = UDim2.new(1, 0, 0, 30)
    Title.Position = UDim2.new(0, 0, 0, 0)
    Title.BackgroundTransparency = 1
    Title.Text = "DNK"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextScaled = true
    Title.Font = Enum.Font.SourceSansBold
    Title.Parent = Frame

    local ToggleButton = Instance.new("TextButton")
    ToggleButton.Size = UDim2.new(0.8, 0, 0, 40)
    ToggleButton.Position = UDim2.new(0.1, 0, 0.3, 0)
    ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    ToggleButton.Text = "OFF"
    ToggleButton.TextColor3 = Color3.fromRGB(255, 0, 0)
    ToggleButton.TextScaled = true
    ToggleButton.Font = Enum.Font.SourceSans
    ToggleButton.Parent = Frame

    local StealthButton = Instance.new("TextButton")
    StealthButton.Size = UDim2.new(0.8, 0, 0, 40)
    StealthButton.Position = UDim2.new(0.1, 0, 0.5, 0)
    StealthButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    StealthButton.Text = "Stealth OFF"
    StealthButton.TextColor3 = Color3.fromRGB(255, 0, 0)
    StealthButton.TextScaled = true
    StealthButton.Font = Enum.Font.SourceSans
    StealthButton.Parent = Frame

    -- Hiệu ứng đổi màu cho tiêu đề
    local function ChangeTitleColor()
        while true do
            local tweenInfo = TweenInfo.new(1, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
            local colors = {
                Color3.fromRGB(255, 0, 0),
                Color3.fromRGB(0, 255, 0),
                Color3.fromRGB(0, 0, 255),
                Color3.fromRGB(255, 255, 0)
            }
            for _, color in ipairs(colors) do
                TweenService:Create(Title, tweenInfo, {TextColor3 = color}):Play()
                task.wait(1)
            end
        end
    end
    task.spawn(ChangeTitleColor)

    -- Xử lý nút bật/tắt CamLock
    ToggleButton.MouseButton1Click:Connect(function()
        CamLockEnabled = not CamLockEnabled
        ToggleButton.Text = CamLockEnabled and "ON" or "OFF"
        ToggleButton.TextColor3 = CamLockEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
        if not CamLockEnabled then
            LockedPlayer = nil
            for _, player in pairs(Players:GetPlayers()) do
                if player.Character and player.Character:FindFirstChild("Highlight") then
                    player.Character.Highlight:Destroy()
                end
            end
        end
    end)

    -- Xử lý nút Stealth Mode
    StealthButton.MouseButton1Click:Connect(function()
        StealthMode = not StealthMode
        StealthButton.Text = StealthMode and "Stealth ON" or "Stealth OFF"
        StealthButton.TextColor3 = StealthMode and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
        if StealthMode then
            -- Ẩn camera ngoài POV (tạm thời làm mờ hoặc giới hạn tầm nhìn)
            Camera.FieldOfView = 20 -- Thu hẹp FOV để mô phỏng tàng hình
        else
            Camera.FieldOfView = 70 -- Khôi phục FOV mặc định
        end
    end)

    Gui = ScreenGui
end

-- Tạo Highlight cho mục tiêu
local function CreateHighlight(character)
    if character:FindFirstChild("Highlight") then
        return character.Highlight
    end
    local highlight = Instance.new("Highlight")
    highlight.FillColor = Color3.fromRGB(0, 255, 0)
    highlight.OutlineColor = Color3.fromRGB(0, 255, 0)
    highlight.FillTransparency = 0.5
    highlight.OutlineTransparency = 0
    highlight.Adornee = character
    highlight.Parent = character
    highlight.Enabled = true
    return highlight
end

-- Lấy ping thực tế
local function GetPing()
    return Stats.Network.ServerStatsItem["Data Ping"]:GetValue() or 150
end

-- Tìm mục tiêu trong POV
local function FindTarget()
    local closestPlayer = nil
    local closestScore = math.huge
    local mouse = LocalPlayer:GetMouse()
    local mousePos = Vector2.new(mouse.X, mouse.Y)

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local targetPart = player.Character:FindFirstChild(TargetPartName) or player.Character:FindFirstChild("HumanoidRootPart")
            if targetPart then
                local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                local screenDistance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                local worldDistance = (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) and 
                    (targetPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude or math.huge

                -- Chỉ chọn mục tiêu trong POV và gần tâm
                local score = screenDistance * 0.8 + worldDistance * 0.2
                if onScreen and screenDistance < FOVRadius and score < closestScore then
                    closestScore = score
                    closestPlayer = player
                end
            end
        end
    end
    return closestPlayer
end

-- Theo dõi hồi sinh
local function MonitorPlayerRespawn(player)
    if not player then return end
    player.CharacterAdded:Connect(function(newCharacter)
        if CamLockEnabled and player == LockedPlayer then
            local humanoid = newCharacter:WaitForChild("Humanoid", 5)
            local targetPart = newCharacter:WaitForChild(TargetPartName, 5) or newCharacter:WaitForChild("HumanoidRootPart", 5)
            if humanoid and targetPart and humanoid.Health > 0 then
                CreateHighlight(newCharacter)
            end
        end
    end)
end

-- Xử lý CamLock
RunService.RenderStepped:Connect(function(deltaTime)
    if CamLockEnabled and LocalPlayer.Character then
        -- Kích hoạt Stealth Mode khi lock
        if StealthMode and CamLockEnabled then
            Camera.FieldOfView = 20 -- Giữ FOV hẹp trong POV
        end

        -- Tìm mục tiêu mới nếu cần
        if not LockedPlayer or not LockedPlayer.Character or not LockedPlayer.Character:FindFirstChild("Humanoid") or LockedPlayer.Character.Humanoid.Health <= 0 then
            if LockedPlayer then
                MonitorPlayerRespawn(LockedPlayer)
            else
                LockedPlayer = FindTarget()
                if LockedPlayer then
                    MonitorPlayerRespawn(LockedPlayer)
                end
            end
        end

        -- Khóa vào mục tiêu trong POV
        if LockedPlayer and LockedPlayer.Character and (LockedPlayer.Character:FindFirstChild(TargetPartName) or LockedPlayer.Character:FindFirstChild("HumanoidRootPart")) then
            local targetPart = LockedPlayer.Character:FindFirstChild(TargetPartName) or LockedPlayer.Character.HumanoidRootPart
            local targetPos = targetPart.Position
            local ping = GetPing() / 1000

            -- Dự đoán vị trí
            if LastTargetPosition then
                local velocity = (targetPart.Position - LastTargetPosition) / deltaTime
                targetPos = targetPos + velocity * ping * PredictionFactor
            end
            LastTargetPosition = targetPart.Position

            -- Khóa camera tức thời
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)

            -- Thêm highlight
            if not targetPart.Parent:FindFirstChild("Highlight") then
                CreateHighlight(targetPart.Parent)
            end
        else
            -- Xóa highlight cho các nhân vật khác
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LockedPlayer and player.Character and player.Character:FindFirstChild("Highlight") then
                    player.Character.Highlight:Destroy()
                end
            end
        end
    else
        -- Khôi phục FOV và xóa highlight khi tắt
        if StealthMode then
            Camera.FieldOfView = 70
        end
        for _, player in pairs(Players:GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("Highlight") then
                player.Character.Highlight:Destroy()
            end
        end
    end
end)

-- Khởi tạo GUI
CreateGUI()

-- Thông báo khởi động
StarterGui:SetCore("SendNotification", {
    Title = "DNK CamLock",
    Text = "CamLock Loaded! Press 'E' or use GUI to toggle. Use Stealth for POV mode.",
    Duration = 5
})
